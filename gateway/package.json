{
  "name": "soc-triage-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Architecture-2 Cloudflare gateway. WebSocket termination per analyst via Durable Objects; subscribes to NATS for triage events and revocations.",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nats-io/nats-core": "^3.0.0-32",
    "@nats-io/transport-node": "^3.0.0-29"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.86.0"
  }
}
